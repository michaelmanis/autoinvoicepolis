
## Reduce Logo Sizes in the Sidebar

Two logo images need to be made smaller:

1. **Collapsed state** (line 133) — the `consultingIcon` square icon:  
   `h-8 w-8` → `h-6 w-6`

2. **Expanded state** (line 144) — the full `polisLogo`:  
   `h-16` → `h-12`

No other changes are needed.
